<h1>Listing trilhas</h1>

<table>
  <thead>
    <tr>
      <th>Nivel</th>
      <th>Tagassunto</th>
      <th>Codigoturma</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @trilhas.each do |trilha| %>
      <tr>
        <td><%= trilha.nivel %></td>
        <td><%= trilha.tagAssunto %></td>
        <td><%= trilha.codigoTurma %></td>
        <td><%= link_to 'Show', trilha %></td>
        <td><%= link_to 'Edit', edit_trilha_path(trilha) %></td>
        <td><%= link_to 'Destroy', trilha, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nova Trilha', new_trilha_path %>

<%= link_to 'Voltar', professors_path %>